# Repository playwrite_tests, AI usage report

The parts where AI aided the design - Claude used:

- The documentation in the README.md is partly produced using Sonnet 5, e.g. the description of starting from a clean slate until running the actual test start command. The produce is slightly tweaked also after reviewing.
