# Repository playwrite_tests, AI usage report

The parts where AI aided the design:

- The documentation is partly produced using Claude Sonnet 5, e.g. the README.md description of starting the tests from clean slate until running the actual start command
